# 2026-08-02 工作日志

## 直播豆（智播豆）工程接入 Git 版本管理
- 在 `E:\zhibodou` 执行 `git init`，完成首次提交 `57f28bc`，默认分支 `master`。
- 提交内容：`.gitignore`、`requirements.txt`、`zhibodou.py`（共 1705 行 / 1659 行 Python）。
- `.gitignore` 已忽略：Python 缓存/虚拟环境/构建产物、本地授权数据（`.ZhiBoDouData/`、`license.lic`、`phone_cfg.json`）、`.workbuddy/` 项目数据、IDE 文件。
- 工程概况：PyQt5 + OpenCV + sounddevice + pyvirtualcam + streamlink 实现的直播推流伴侣；含主播模式（摄像头/抖音直播源采集推流）与观众模式（拉流到虚拟摄像头+音频）。
- 注意：代码内含硬编码 `RELAY_SERVER_IP`、`SECRET_KEY`（"ZhiBoDou2026"，仅为混淆密钥，非真实密钥）、本地局域网 `BRIDGE_IP`。后续如需推到远程仓库，应确认这些信息是否适宜公开。
- Git 账号：黄敬佩 <hjp9221@163.com>（已配置，无需设置）。

## 修复登录后左侧控制面板组件显示不全
- 现象：进入主播页后，左侧麦克风、美颜、授权等区域文字/滑块/按钮被压缩、显示不全（用户截图确认）。
- 根因：主窗口固定 `1280x720`，左侧 `QFrame` 内控件总最小高度超过可用垂直空间（约 690px），`QLayout` 强行压缩子控件。
- 修复：为主播页 `left_host` 和观众页 `left_client` 分别包裹 `QScrollArea`，隐藏水平滚动条，仅保留垂直滚动；移除两侧 `left_lay.addStretch()` 避免滚动底部空白。
- 提交：`e92f7f1` "修复主播/观众页左侧控制面板溢出导致组件显示不全"。
- 验证：`python -m py_compile zhibodou.py` 语法通过。
- 备注：因环境无图形显示，未实际启动 GUI 验证渲染效果；如滚动条仍不理想，可进一步把"返回首页"按钮固定在滚动区域外。

## 优化左侧滚动面板视觉：滚动条融入面板
- 现象：用户反馈滚动条与左侧控制面板割裂，且左右间距过大。
- 根因：第一次修复直接把 `QScrollArea` 放进布局，滚动条显示在 `QFrame` 边框外侧；`host_lay` / `client_lay` 间距 20，导致预览区离左侧面板过远。
- 修复：
  - 主播页 `left_host`、观众页 `left_client` 由 `QFrame` 改为 `QWidget`
  - 新增 `QFrame` 作为视觉外框，把 `QScrollArea` 包裹在内，使滚动条位于面板边框内部
  - 滚动条样式改为 8px 透明轨道 + 蓝色滑块（无边框、无上下箭头），与深色面板融合
  - 左右布局间距从 20 缩到 12
  - 内层美颜/授权框仍保留原有 `QFrame` 样式
- 提交：`7a4ca21` "优化左侧滚动面板视觉：滚动条融入面板、缩小与预览间距"。
- 验证：`python -m py_compile zhibodou.py` 语法通过。
